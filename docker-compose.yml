version: '3.8'

services:
  discord-bot:
    build: .
    restart: always
    environment:
      - OLLAMA_API_URL=http://host.docker.internal:11434/api/chat
    extra_hosts:
      - "host.docker.internal:host-gateway"